version: '3.9'
services:
  db:
    image: nouchka/sqlite3:latest
    volumes:
      - ./venv/data/db:/root/db
    restart: always
    container_name: myapp_db
    environment:
      - SQLITE3_DB=db.sqlite3








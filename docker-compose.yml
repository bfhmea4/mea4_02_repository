version: '3.9'
services:
  db:
    image: nouchka/sqlite3:latest
    volumes:
      - ./data/db:/root/db
    environment:
      - SQLITE3_DB=db.sqlite3
    command:
      - ./database/sqlite_create.py
      #- uvicorn main:app --reload



